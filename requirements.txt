# Основной веб-фреймворк
Flask~=3.1.1

# WSGI сервер для запуска в проде
gunicorn~=21.2.0

# Работа с формами и CSRF-защитой
flask-wtf~=1.2.1

# Аутентификация пользователей (если понадобится)
flask-login~=0.6.3

# Работа с базой данных (если используешь)
flask-sqlalchemy~=3.1.1

# Миграции базы данных (если используешь Alembic)
flask-migrate~=4.0.5

# Поддержка .env и конфигурации
python-dotenv~=1.0.1

# Для генерации случайных ссылок и безопасности
itsdangerous~=2.1.2

# Для валидации данных (если используешь)
email-validator~=2.1.1

# Логгирование и отладка
colorlog~=6.8.2
