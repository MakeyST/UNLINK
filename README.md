# 🧨 Flask-приложение для генерации одноразовых ссылок на скачивание файлов

## 📦 Описание

Это простое Flask-приложение позволяет:

- Загружать файлы в папку `uploads/`
- Генерировать одноразовые ссылки на скачивание файла
- Автоматически удалять ссылку после первого скачивания
- Вести лог скачиваний (IP, User-Agent, время, имя файла и UUID ссылки)
- Блокировать докачку через заголовок `Range`

---

## 🚀 Установка и запуск

### 🔧 Установка на Ubuntu (20.04 и выше)

1. **Установите Python и pip (если ещё не установлены):**
   ```bash
   sudo apt update
   sudo apt install python3 python3-pip -y
   ```

2. **Создайте директорию проекта и перейдите в неё:**
   ```bash
   mkdir one_time_link_server
   cd one_time_link_server
   ```

3. **Файлы проекта:**
   - `app.py` — основной Flask-сервер
   - `generate.py` — генератор одноразовых ссылок

4. **Создайте и активируйте виртуальное окружение (опционально):**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

5. **Установите зависимости:**
   ```bash
   pip install flask
   ```

6. **Запустите Flask-сервер:**
   ```bash
   python3 app.py
   ```

---

## 🧪 Генерация одноразовой ссылки

1. Запустите генератор:
   ```bash
   python3 generate.py
   ```

2. Выберите нужный файл и получите ссылку, например:
   ```
   ✅ Одноразовая ссылка создана:

   http://localhost:5000/download/fc5e6c32-42ab-438c-8fa5-ec06d6a3cd08
   ```

---

## 🌐 Доступ извне (на сервере Ubuntu)

1. **Откройте порт 5000 в firewall (если включен):**
   ```bash
   sudo ufw allow 5000
   ```

2. **Запустите сервер с параметром `host='0.0.0.0'`:**

   Откройте `app.py` и измените последнюю строку:
   ```python
   app.run(host='0.0.0.0', debug=False)
   ```

3. **Получите внешний IP сервера:**
   ```bash
   curl ifconfig.me
   ```

4. **Теперь ссылка будет вида:**
   ```
   http://80.85.241.112:5000/download/<UUID>
   ```

---

## 🧑‍💻 Автор

**MakeyDev**  
Telegram: [@MakeyStar](https://t.me/MakeyStar)